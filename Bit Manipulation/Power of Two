class Solution:
    def isPowerOfTwo(self, n: int) -> bool:
        # n given here is integer so can be negative and power of 2 can't be negative
        return n>0 and (n&(n-1)==0)
        '''
        if n == 0:
            return False
        
        return n & (n-1) == 0 
        '''
